global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "trading-bot"
    metrics_path: /actuator/prometheus
    basic_auth:
      username: "__AUTH_USER__"
      password_file: /tmp/trading-bot-prometheus-password
    static_configs:
      - targets: ["app:8080"]
